Demo bundle: pages produced by a real headless run on the anonymised report that
ships with this project. Screenshots are downscaled for the web, so this copy
does not match the evidence manifest of the sealed case it came from; the case
itself is unchanged. Rebuild with `scripts/make_demo.py` in the project repo.
